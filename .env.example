# ===========================================
# JFinder Configuration
# ===========================================

# Scopus API (Optional - falls back to OpenAlex if not provided)
# Get your key at: https://dev.elsevier.com/
# If not set, JFinder will use OpenAlex (free, no key required)
SCOPUS_API_KEY=

# OpenRouter API (Required)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct

# Proxy Service (Optional - for better download reliability)
# WebShare.io proxy list URL
PROXY_URL=

# Download Sources
# By default, these free sources run in PARALLEL for faster downloads:
# - OpenAlex OA URLs (free, no key)
# - Unpaywall (free, no key)
# - Sci-Hub (free, no key)
# - LibGen (free, no key)
#
# Anna's Archive (Optional - adds one more fallback source)
# Set either key to enable Anna's Archive as additional source
ANNAS_API_KEY=
RAPIDAPI_KEY=

# Application Settings
DOWNLOAD_DIR=./downloads
MAX_UPLOAD_SIZE=16777216
TASK_TTL_MS=3600000

# Server (set by Coolify or manually)
HOST=0.0.0.0
PORT=3000

# Analytics (Optional - Umami)
UMAMI_WEBSITE_ID=
UMAMI_SRC=
